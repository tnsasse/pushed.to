extends layout

block page_parameters
  - var page_brand_url= '/'
  - var page_title= 'pushed.to'

block page_content
  h1 ¯\_(ツ)_/¯  #{error.status} #{message}
  p 
  if error && error.stack
    pre #{error.stack}
